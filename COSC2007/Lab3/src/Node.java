class Node {
    int key;
    //left and right nodes of the tree
    Node left, right;

    public Node(int item) {
        key = item;
        //left and right nodes initialized to null
        left = right = null;
    }
}